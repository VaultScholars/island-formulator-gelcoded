<% content_for :title, "Editing batch" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing batch</h1>

  <%= render "form", batch: @batch %>

  <%= link_to "Show this batch", @batch, class: "rounded-lg py-3 px-5 bg-purple-50 text-purple-600 hover:bg-purple-100 font-medium transition-colors" %>
  <%= link_to "Back to batches", batches_path, class: "rounded-lg py-3 px-5 bg-purple-50 text-purple-600 hover:bg-purple-100 font-medium transition-colors" %>
